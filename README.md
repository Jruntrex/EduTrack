# EduTrack - Система управління освітнім процесом

**Сучасна структура на MySQL + Django 5.2**

---

## 🚀 Швидкий старт

### 1. Клонувати та налаштувати

```bash
git clone <repo>
cd Django
python -m venv .venv
.venv\Scripts\activate  # Windows
pip install django==5.2.7 mysql-connector-python
```

### 2. Міграції та сервер

```bash
python manage.py migrate
python manage.py runserver
```

Додаток: **http://localhost:8000/**  
Admin: **http://localhost:8000/admin/**

---

## 📊 Структура БД

```
TeachingAssignment (Викладач + Предмет + Група)
    ↓
EvaluationType (Типи оцінювання)
    ↓
LessonSession (Конкретні уроки)
    ↓
StudentPerformance (Оцінки та пропуски)
```

---

## 🗄️ Налаштування MySQL

**Поточні параметри:**
```
HOST: localhost
PORT: 3306
USER: root
PASSWORD: sq123ghreQ6509
DATABASE: edutrack_db
CHARSET: utf8mb4
```

**Файл конфігурації:** `edutrack_project/settings.py`

---

## 📂 Структура проекту

```
Django/
├── edutrack_project/      # Налаштування
│   ├── settings.py        # MySQL конфіг
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── main/                  # Додаток
│   ├── models.py          # ✨ Нові моделі
│   ├── views.py           # ⚠️ Потребує оновлення
│   ├── forms.py
│   ├── admin.py
│   ├── urls.py
│   ├── migrations/        # БД міграції
│   ├── templates/         # HTML шаблони
│   ├── static/            # CSS/JS
│   └── tests.py
├── manage.py              # Django CLI
├── SETUP_COMPLETE.md      # Статус встановлення
└── MIGRATION_TO_MYSQL.md  # Деталі переходу
```

---

## 💻 Основні команди

```bash
# Створити міграції
python manage.py makemigrations

# Застосувати міграції
python manage.py migrate

# Перевірити налаштування
python manage.py check

# Admin панель
python manage.py createsuperuser
python manage.py runserver

# Django shell
python manage.py shell
```

---

## 📋 Моделі

### Базові сутності
- `StudyGroup` - групи (КН-41, КН-42 і т.д.)
- `User` - користувачі (студенти, викладачі, адміни)
- `Subject` - предмети (Математика, Програмування)
- `AbsenceReason` - причини пропусків

### Навчальний процес
- `TeachingAssignment` - назначення (основна таблиця)
- `EvaluationType` - типи оцінювання
- `WeeklySchedule` - розклад
- `LessonSession` - проведені уроки

### Успішність
- `StudentPerformance` - оцінки та пропуски

---

## ⚠️ Статус розробки

### ✅ Готово
- Нова структура моделей
- MySQL БД
- Адмін панель
- Міграції

### 🔧 Потребує роботи
- **views.py** - оновлення для нової структури
- Хеширування паролів
- API (Django REST Framework)
- Тести
- Фронтенд

---

## 📖 Детально

Див. файли:
- `SETUP_COMPLETE.md` - статус завершення
- `MIGRATION_TO_MYSQL.md` - про MySQL

---

**Успіху! 🚀**
