# 🔄 Керівництво з переходу на MySQL

## Розраховуємо, що ви використовуєте нову структуру моделей!

### 📝 Що змінилося в структурі

**Стара структура:**
- `JournalEntry` - таблиця з суміішеними даними
- `AbsenceType` - тип пропусків з числовим кодом
- `Subject` мала посилання на учителя

**Нова структура (MySQL-сумісна):**
- `TeachingAssignment` - зв'язок: Предмет + Викладач + Група
- `EvaluationType` - типи занять для кожного призначення
- `LessonSession` - конкретні проведені уроки
- `StudentPerformance` - оцінки та пропуски студентів
- `AbsenceReason` - причини пропусків з кодом (текст)
- `Subject` - незалежний довідник предметів
- `WeeklySchedule` - розклад з посиланнями на TeachingAssignment

---

## 🚀 Інструкції з переходу

### Варіант 1: Локальна розробка (SQLite) ✅ ГОТОВО

SQLite конфіг вже налаштований. БД готова!

```bash
# Перевірити стан
python manage.py check

# Запустити тести
python manage.py test

# Запустити сервер
python manage.py runserver
```

---

### Варіант 2: Перехід на Production MySQL

#### Крок 1: Встановити MySQL та драйвер

```bash
# Windows - завантажити MySQL Community Edition
# https://dev.mysql.com/downloads/mysql/

# Ubuntu/Debian
sudo apt-get install mysql-server mysql-client

# Або через Docker
docker run -d --name mysql -e MYSQL_ROOT_PASSWORD=root -p 3306:3306 mysql:8.0
```

#### Крок 2: Встановити MySQL драйвер для Python

```bash
pip install mysql-connector-python
```

#### Крок 3: Оновити `settings.py`

```python
DATABASES = {
    'default': {
        'ENGINE': 'mysql.connector.django',
        'NAME': 'edutrack_db',
        'USER': 'root',
        'PASSWORD': 'your_password',  # Змінити!
        'HOST': '127.0.0.1',
        'PORT': '3306',
        'OPTIONS': {
            'charset': 'utf8mb4',
            'use_unicode': True,
        },
    }
}
```

#### Крок 4: Створити БД на MySQL

**Вручну через SQL:**
```sql
CREATE DATABASE edutrack_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

**Через Python:**
```python
python setup_mysql.py
```

**Через Django:**
```bash
python manage.py migrate
```

#### Крок 5: Експортувати дані (якщо потрібно)

```bash
# Експортувати з SQLite
python manage.py dumpdata > backup.json

# Імпортувати у MySQL
python manage.py migrate --run-syncdb
python manage.py loaddata backup.json
```

---

## 📊 Схема БД (MySQL)

```
┌─────────────────────────────────────────────┐
│              tbl_users                       │
│ id | full_name | email | role | group_id   │
└─────────────────────────────────────────────┘
         ↓              ↓
    ┌────────────────────────────┐
    │   tbl_study_groups         │
    │   id | name                │
    └────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│     tbl_teaching_assignments                         │
│ id | subject_id | teacher_id | group_id             │
└──────────────────────────────────────────────────────┘
      ↓              ↓             ↓
   ┌──────────┐  ┌──────────┐  ┌──────────┐
   │ Subject  │  │ User     │  │ Group    │
   └──────────┘  └──────────┘  └──────────┘
      ↓
┌──────────────────────────────────────────────────┐
│     tbl_evaluation_types                          │
│ id | assignment_id | name | weight_percent      │
└──────────────────────────────────────────────────┘
      ↓
┌──────────────────────────────────────────────────┐
│     tbl_lesson_sessions                           │
│ id | assignment_id | date | evaluation_type_id  │
└──────────────────────────────────────────────────┘
      ↓
┌────────────────────────────────────────────────────┐
│  tbl_student_performance                           │
│ id | lesson_id | student_id | grade | absence_id │
└────────────────────────────────────────────────────┘
      ↓              ↓              ↓
   ┌──────────┐  ┌──────────┐  ┌────────────┐
   │ Lesson   │  │ User     │  │ Absence    │
   └──────────┘  └──────────┘  └────────────┘
```

---

## 🔑 Ключові особливості нової структури

### 1. **TeachingAssignment** - основна таблиця зв'язку
```
Адмін створює: "Викладач Петренко читає Математику у групи КН-41"
↓
Це один запис у TeachingAssignment
↓
На основі цього створюється розклад (WeeklySchedule)
```

### 2. **EvaluationType** - гнучка конфігурація оцінювання
```
Кожен викладач для свого предмету + групи може мати:
- Лекція (20% от оцінки)
- Практика (30%)
- Екзамен (50%)
```

### 3. **LessonSession** - конкретний урок
```
На кожному уроці викладач вказує:
- Дату проведення
- Тип цього конкретного заняття (напр., "Сьогодні екзамен")
- Тему
```

### 4. **StudentPerformance** - гнучкий запис успішності
```
Один запис може містити:
- Оцінку (і студент присутній)
- Пропуск (і студент відсутній)
- Оцінку ТА пропуск (студент був, але не писав)
```

---

## 🔍 Приклади запитів

### Всі оцінки студента за предметом
```python
from main.models import StudentPerformance, User

student = User.objects.get(full_name="Іван Петров")
grades = StudentPerformance.objects.filter(
    student=student,
    lesson__assignment__subject__name="Математика"
).select_related('grade', 'absence')
```

### Середня оцінка групи за предметом
```python
from django.db.models import Avg
from main.models import StudentPerformance, StudyGroup

group = StudyGroup.objects.get(name="КН-41")
avg_grade = StudentPerformance.objects.filter(
    student__group=group,
    lesson__assignment__subject__name="Математика"
).aggregate(Avg('grade'))
```

### Кількість пропусків студента
```python
from main.models import StudentPerformance

student = User.objects.get(full_name="Іван Петров")
absences = StudentPerformance.objects.filter(
    student=student,
    absence__isnull=False
).count()
```

---

## ⚠️ Поточні обмеження

1. **User модель** - не використовує Django Auth
   - Можна інтегрувати з Django AbstractBaseUser у майбутньому

2. **Паролі** - зберігаються як plain text (для тестування)
   - Потрібно реалізувати хеширання!

3. **Views** - потребують оновлення для нової структури
   - Старі view-функції посилаються на JournalEntry і AbsenceType

---

## 🛠️ Наступні кроки

1. **Оновити views.py** для нової структури
2. **Оновити шаблони** для нових моделей
3. **Імплементувати хеширування паролів**
4. **Написати API** для фронтенду
5. **Створити тести** для основних функцій

---

## 📞 Налаштування підключення MySQL

### Для Windows

```ini
# Запустити MySQL Services
# або через командний рядок:
# cd "C:\Program Files\MySQL\MySQL Server 8.0\bin"
# mysqld --install
# net start MySQL80
```

### Для Docker

```bash
docker run -d --name edutrack-mysql \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=edutrack_db \
  -p 3306:3306 \
  mysql:8.0
```

### Для Linux/macOS

```bash
brew install mysql
brew services start mysql
mysql -u root -p

# У MySQL:
CREATE DATABASE edutrack_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

**Успіху з переходом на MySQL! 🚀**
