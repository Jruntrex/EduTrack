# Інструкції по заповненню бази даних тестовими даними

## Варіант 1: Через Python скрипт (РЕКОМЕНДОВАНО)

Цей спосіб автоматично створює правильні хеші паролів та використовує Django ORM.

### Кроки:

1. Переконайтеся, що віртуальне середовище активоване:
```powershell
.\venv\Scripts\Activate.ps1
```

2. Запустіть скрипт заповнення:
```powershell
python populate_db.py
```

### Переваги:
- ✅ Автоматичне хешування паролів через Django
- ✅ Валідація даних через моделі
- ✅ Безпечне створення даних
- ✅ Детальна статистика після виконання

---

## Варіант 2: Через SQL скрипт

Якщо потрібно використати прямі SQL запити.

### Кроки:

1. Відкрийте MySQL клієнт або phpMyAdmin

2. Виконайте SQL скрипт:
```powershell
mysql -u root -p edutrack_db < test_data.sql
```

### Важливо:
⚠️ У SQL скрипті використані приклади хешів паролів. Для реального використання потрібно згенерувати правильні хеші.

Для генерації правильного хешу пароля:
```python
from django.contrib.auth.hashers import make_password
print(make_password('password123'))
```

---

## Тестові дані для входу

Після заповнення бази даних ви можете використовувати наступні облікові записи:

### Адміністратор:
- Email: `admin@edutrack.com`
- Пароль: `password123`

### Викладачі:
- Email: `petrenko@edutrack.com` / Пароль: `password123`
- Email: `sydorenko@edutrack.com` / Пароль: `password123`
- Email: `kovalenko@edutrack.com` / Пароль: `password123`
- Email: `shevchenko@edutrack.com` / Пароль: `password123`

### Студенти:
- Email: `bondarenko.o@student.com` / Пароль: `password123`
- Email: `melnyk.a@student.com` / Пароль: `password123`
- Email: `tkachenko.d@student.com` / Пароль: `password123`
- Email: `lysenko.k@student.com` / Пароль: `password123`
- Email: `hrytsenko.m@student.com` / Пароль: `password123`

---

## Що включено в тестові дані:

### 📚 Групи (4):
- КН-41, КН-42, ІП-31, ІП-32

### 👥 Користувачі (15):
- 1 адміністратор
- 4 викладачі
- 10 студентів (розподілені по групах)

### 📖 Предмети (6):
- Вища математика
- Програмування
- Бази даних
- Веб-технології
- Алгоритми та структури даних
- Операційні системи

### 👨‍🏫 Призначення викладачів (7):
- Різні комбінації викладачів, предметів та груп

### 📊 Типи оцінювання:
- Лекції, Практики, Лабораторні, Екзамени, Проекти
- З різними вагами (10%-70%)

### 📅 Розклад:
- Щотижневий розклад для груп КН-41 та КН-42

### 📝 Проведені заняття:
- Заняття за останні 2 тижні (січень 2026)
- З різними темами та типами

### ❌ Причини пропусків (4):
- Н - Неповажна причина
- Б - Хвороба
- В - Відрядження
- П - Поважна причина

### 📈 Успішність студентів:
- Оцінки від 65 до 100 балів
- Деякі пропуски з різними причинами
- Коментарі викладачів

---

## Перевірка даних

Після заповнення можна перевірити дані через Django shell:

```powershell
python manage.py shell
```

```python
from main.models import *

# Перевірка кількості записів
print(f"Груп: {StudyGroup.objects.count()}")
print(f"Користувачів: {User.objects.count()}")
print(f"Предметів: {Subject.objects.count()}")
print(f"Занять: {LessonSession.objects.count()}")
print(f"Оцінок: {StudentPerformance.objects.count()}")

# Перевірка конкретного студента
student = User.objects.get(email='bondarenko.o@student.com')
print(f"\nСтудент: {student.full_name}")
print(f"Група: {student.group.name}")

# Перевірка оцінок студента
grades = StudentPerformance.objects.filter(student=student)
print(f"Кількість оцінок: {grades.count()}")
for g in grades[:3]:
    print(f"  - {g.lesson.date}: {g.grade} ({g.lesson.evaluation_type.name})")
```

---

## Очищення бази даних

Якщо потрібно очистити всі дані та почати заново:

```powershell
python manage.py flush
```

Або запустіть `populate_db.py` знову - він автоматично очистить таблиці перед заповненням.
